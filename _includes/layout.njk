<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/css/index.css"> {# Ajoute ton CSS global ici #}
</head>
<body>
  <nav>
    <ul>
      {% for entry in collections.eleventyNavigation %}
        <li>
          <a href="{{ entry.url }}" class="{% if page.url == entry.url %}active{% endif %}">
            {{ entry.title }}
          </a>
        </li>
      {% endfor %}
    </ul>
  </nav>

  <main>
    {{ content | safe }}
  </main>
</body>
</html>
