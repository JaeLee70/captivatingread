<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      margin: 0 auto;
      max-width: 800px;
      padding: 2rem;
      background-color: #fefefe;
      color: #333;
    }
    h1 {
      color: #ba3b46;
    }
    nav {
      margin-bottom: 2rem;
    }
    nav ul {
      padding: 0;
      list-style: none;
      display: flex;
      justify-content: center;
      gap: 2rem;
      margin: 0;
    }
    nav li {
      margin: 0;
    }
    nav a {
      color: #ba3b46;
      text-decoration: none;
      font-weight: bold;
      padding-bottom: 4px;
      border-bottom: 2px solid transparent;
      transition: all 0.3s ease;
    }
    nav a:hover {
      border-bottom: 2px solid #ba3b46;
    }
    nav a.active {
      border-bottom: 2px solid #ba3b46;
    }
    a {
      color: #ba3b46;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <nav>
    <ul>
      {% for entry in collections.all %}
        {% if entry.data.eleventyNavigation %}
          <li>
            <a href="{{ entry.url }}" class="{% if page.url == entry.url %}active{% endif %}">
              {{ entry.data.eleventyNavigation.key }}
            </a>
          </li>
        {% endif %}
      {% endfor %}
    </ul>
  </nav>

  <main>
    {{ content | safe }}
  </main>
</body>
</html>
