<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      margin: 0 auto;
      max-width: 800px;
      padding: 2rem;
      background-color: #fefefe;
      color: #333;
    }
    h1 {
      color: #ba3b46;
    }
    a {
      color: #ba3b46;
      text-decoration: none;
    }
    a:hover {
      text-decoration: underline;
    }
    nav ul {
      padding: 0;
      list-style: none;
      display: flex;
      gap: 1rem;
    }
  </style>
</head>
<body>
  <nav>
    <ul>
      {% for entry in collections.eleventyNavigation %}
        <li><a href="{{ entry.url }}">{{ entry.data.eleventyNavigation.key }}</a></li>
      {% endfor %}
    </ul>
  </nav>

  <main>
    {{ content | safe }}
  </main>
</body>
</html>

